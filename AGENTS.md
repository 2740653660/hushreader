# 项目代理协作规则

本文件只记录长期稳定的协作和工作规则。产品需求、版本范围、技术决定和当前进度统一维护在 `docs/` 中，不在这里重复。

## 产品负责人的身份与分工

- 产品负责人是编程初学者，不负责理解代码、选择底层技术或执行开发命令。
- 产品负责人负责表达想法、使用场景、期望体验、优先级和对成品的判断。
- AI 代理负责把想法整理为明确需求，并承担技术调研、方案选择、架构设计、编码、测试、打包、发布准备和后续维护落地。
- 不得把本应由工程人员判断的技术问题原样抛给产品负责人。需要选择时，先用日常语言解释它对体验、成本、风险和进度的影响，再给出明确推荐。
- 提问应少而聚焦，优先询问“用户希望怎样使用”和“什么结果最重要”，避免要求产品负责人填写技术参数。
- 产品负责人拥有最终产品决定权；代理拥有已确认范围内的工程实施责任。

## 沟通方式

- 默认使用清晰、易懂的中文，不假设产品负责人掌握编程术语。
- 必须使用技术词时，紧接着说明它对用户或项目意味着什么。
- 先说明结论、影响和建议，再补充必要的技术原因。
- 发现需求矛盾、成本冲突或风险时要主动指出，并提出可以落地的解决办法。
- 不因产品负责人缺少编程经验而降低工程质量，也不得用技术复杂性替代解释。

## 开始工作前

按顺序阅读以下持续更新的文档：

1. `docs/PRODUCT.md`：当前确认的产品需求和范围。
2. `docs/DECISIONS.md`：已经确认的决定及其历史。
3. `docs/ROADMAP.md`：开发阶段和功能安排。
4. `docs/PROGRESS.md`：当前进度、阻塞和下一项已获批准的工作。

内容冲突时，优先级如下：

1. 产品负责人最新的明确指示。
2. `docs/DECISIONS.md` 中较新的决定。
3. `docs/PRODUCT.md`。
4. `docs/ROADMAP.md`。
5. `docs/PROGRESS.md`。
6. 现有代码行为。

## 开发授权边界

- 需求讨论中的“继续”不等于授权修改产品代码。
- 产品负责人明确说“开始开发”“按确认方案开发”等话语前，只能检查、分析、讨论、验证可行性和更新文档。
- 文档修改必须符合产品负责人的明确要求；不能借更新文档之名提前实施产品功能。
- 如果已批准的开发范围发生实质变化，实施前重新说明范围并获得确认。
- 已批准范围内的常规实现、测试和修复由代理自主完成，不把每个技术细节重新交给产品负责人决定。

## 文档维护

- `docs/` 下的文件代表项目当前共识，必须随项目持续更新。
- 需求变化时更新 `docs/PRODUCT.md`，并在 `docs/DECISIONS.md` 追加决定。
- 功能调整开发阶段时更新 `docs/ROADMAP.md`。
- 开始、完成、暂停或阻塞工作时更新 `docs/PROGRESS.md`。
- 被推迟或否决的想法也要保留在适当位置，避免反复讨论。
- 不得擅自把待确认问题变成正式需求。
- `docs/DECISIONS.md` 采用追加式记录；新决定取代旧决定时注明关系，不删除历史。

## CodeGraph

- 如果仓库根目录存在 `.codegraph/`，理解或定位代码前优先使用 CodeGraph。
- MCP 工具可用时优先调用 `codegraph_explore`；否则运行 `codegraph explore "<问题>"`。
- 指定文件或符号可以让 CodeGraph 返回带行号的当前源码和调用路径。
- 索引状态异常或代码发生较大变化时，先运行 `codegraph status`，再按需要运行 `codegraph sync`。
- 不存在 `.codegraph/` 时才使用常规搜索工具进行代码定位。

## 仓库与代码工作规则

- 修改前先理解现有实现和仓库约定，优先沿用已有模式。
- 保留产品负责人或其他工作留下的改动，不得擅自回滚无关内容。
- 避免破坏性 Git 或文件操作；删除或覆盖重要内容前确认目标和影响。
- 修改范围保持聚焦，不夹带无关重构。
- 测试范围与风险匹配，关键用户流程必须做端到端验证。
- 无法执行的测试、缺少的环境和剩余风险必须明确记录。

## 编译与构建

- 涉及编译构建时，如果 Windows 目标产物可以在产品负责人的 Windows 电脑上完成，**优先由产品负责人在 Windows 上编译**，代理不得直接在本机（Linux 服务器）交叉编译。
- 代理应主动判断“哪些编译需要走 Windows”，并及时告知产品负责人，附上可直接复制的命令和预计耗时，由产品负责人执行。
- 确需本机编译且无 Windows 环境可用时，先向产品负责人说明原因并获得同意，再动手。

## 发布与密钥管理

发布与密钥规则长期固定，每次发布新版本都遵循本节（与 `scripts/prepare-release.ps1` 配套）。

### 发布由代理操作

- GitHub 发布操作由 AI 代理执行（创建 Release、上传产物、验证），产品负责人不执行发布命令。
- 前提：`gh` 已登录产品负责人的 GitHub 账号（`gh auth status` 确认）。仓库为 `2740653660/hushreader`。
- 推送与 GitHub 操作走既有代理（127.0.0.1:7897，git 仓库已配置局部代理）。

### 固定发布流程（每次新版走同一套）

0. **前置检查（易漏）**：仓库必须是公开（`gh repo view <repo> --json visibility` 确认是 PUBLIC）。私有仓库时应用内更新对普通用户全是 404，无法使用；公开动作需产品负责人确认（对外可见源码）。本次首次发布时发现仓库为 PRIVATE，经产品负责人确认后公开（2026-08-12）。
1. 更新版本号：`src-tauri/tauri.conf.json` 的 `version`（安装包文件名随版本号变化）；更新 `CHANGELOG.md` 的更新说明。
2. Windows 本机构建（由产品负责人在 Windows 执行，代理给出命令）：
   - 设置签名环境变量：`TAURI_SIGNING_PRIVATE_KEY`（私钥文件路径或内容）、`TAURI_SIGNING_PRIVATE_KEY_PASSWORD`。
   - 运行 `npx tauri build`（走代理环境变量；JRE 大文件被 Defender 锁定时按既有方法处理）。
   - 产物应包含 `bundle/nsis/HushReader_x.y.z_x64-setup.exe` 及其 `.sig` 签名文件。
3. 生成更新清单：运行 `.\scripts\prepare-release.ps1`，产出 `latest.json`（含版本号、说明、签名、下载地址）。
4. 推送代码；用 `gh` 创建 GitHub Release（tag = `v版本号`，正文填更新说明），上传 3 个文件：
   - `HushReader_x.y.z_x64-setup.exe`
   - 对应的 `.sig`
   - `latest.json`
5. 发布后验证：`https://github.com/2740653660/hushreader/releases/latest` 能显示新版本；`latest.json` 的版本号与签名可被应用内更新正常校验（关键流程必须端到端实测一次）。
6. 同步更新 `docs/`（PROGRESS 等）。

### 签名密钥规则（Tauri 更新强制 ed25519 签名，不可关闭）

- 私钥 = 私钥文件 + 密码，仅保存在产品负责人本机（默认 `C:\Users\yy\.hushreader-update-key\`）与其信任的私人备份处（网盘/密码管理器）。
- **私钥和密码绝不进入项目仓库、不进 Git、不上传 GitHub**；公钥写入 `src-tauri/tauri.conf.json` 的 `plugins.updater.pubkey` 随代码走。
- **私钥丢失 = 老用户永远无法升级**，这是唯一必须防的风险；向产品负责人强调备份。
- 换电脑：把私钥文件复制到新电脑并记住密码即可继续发布，公钥已在仓库无需重配。
- 生成命令（Windows，用 `npx` 时不需要 `--`）：`npx tauri signer generate -w <私钥路径>`；公钥输出在终端，需人工填入 `tauri.conf.json`。

## 完成标准

一项已批准的工作只有同时满足以下条件才算完成：

- 已确认的用户流程能够完整工作。
- 加载、空状态、错误、重试和禁用状态得到处理。
- 相关测试、构建和必要的人工检查通过。
- 未引入已知的数据损坏、安全或升级风险。
- 对应项目文档已经同步更新。
- 向产品负责人说明完成结果、验证情况和仍然存在的限制。
